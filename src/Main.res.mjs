// Generated by ReScript, PLEASE EDIT WITH CARE

import * as PixiJs from "pixi.js";
import * as Primitive_option from "rescript/lib/es6/Primitive_option.js";

async function main() {
  let app = new PixiJs.Application();
  await app.init({
    background: "rebeccapurple",
    resizeTo: Primitive_option.some(window)
  });
  document.body.appendChild(app.canvas);
  let texture = await PixiJs.Assets.load("https://pixijs.com/assets/bunny.png");
  let bunny = new PixiJs.Sprite(texture);
  bunny.anchor.set(0.5);
  bunny.x = app.screen.width / 2 | 0;
  bunny.y = app.screen.height / 2 | 0;
  app.stage.addChild(bunny);
  app.ticker.add(time => {
    bunny.rotation = bunny.rotation + 0.025 * time.deltaTime;
  });
  console.log("Game rendered!");
}

main();

export {
  main,
}
/*  Not a pure module */
