// Generated by ReScript, PLEASE EDIT WITH CARE

import Kaplay from "kaplay";

let k = Kaplay();

k.loadSprite("squartle", "sprites/squirtle.jpeg", {
  sliceX: 4,
  sliceY: 1
});

let squartle = k.add([
  k.pos(10, 20),
  k.sprite("squartle")
]);

k.onKeyDown(key => {
  k.debug.log(key);
  switch (key) {
    case "left" :
      squartle.move(-200, 0);
      return;
    case "right" :
      squartle.move(200, 0);
      return;
    case "up" :
      squartle.move(0, -200);
      return;
    case "down" :
      squartle.move(0, 200);
      return;
    case "space" :
    case "enter" :
      return;
  }
});

let speed = 200;

export {
  k,
  squartle,
  speed,
}
/* k Not a pure module */
