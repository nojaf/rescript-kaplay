---
import { getCollection, getEntry } from 'astro:content';
import StarlightPage from "@astrojs/starlight/components/StarlightPage.astro";
import APIModule from "../components/APIModule.astro";

const apiDocs = await getCollection("apiDocs");

const frontmatter = {
  title: "API references",
};

const headings = [];
console.log(apiDocs[1]);
---

<StarlightPage frontmatter={frontmatter} headings={headings} >
  <!-- <APIModule filePath="/Users/nojaf/Projects/rescript-kaplay/packages/rescript-kaplay/src/Context.res" /> -->
  <pre>{JSON.stringify(apiDocs.find(a => a.id === "Kaplay.Pos"), null, 2)}</pre>
</StarlightPage>
